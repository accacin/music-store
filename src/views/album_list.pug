extends layout

block content
  h1= title

  ul
    each album in album_list
      li
        a(href=album.url) #{album.name} - #{album.artist}
        |  - #{album.category.name}

    else
      li There are no books.
